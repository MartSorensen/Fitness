<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textSize="15dp"
        android:layout_marginTop="40dp"
        android:text="Welcome to Progress fragment"
        android:id="@+id/textTitle">
    </TextView>

    <Button
        android:layout_width="135dp"
        android:layout_height="70dp"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:id="@+id/btnNavHome"
        android:text="Home"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_below="@+id/btnNavWorkouts"
        android:layout_height="wrap_content"
        tools:listitem="@layout/workout_item">
    </androidx.recyclerview.widget.RecyclerView>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnNavHome"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:id="@+id/btnNavGoals"
        android:text="Goals">

    </Button>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_progress"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:listitem="@layout/workout_item">
    </androidx.recyclerview.widget.RecyclerView>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnNavGoals"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:id="@+id/btnNavWorkouts"
        android:text="Workouts">

    </Button>
</RelativeLayout>