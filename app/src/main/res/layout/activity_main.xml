<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

  <TextView
      android:layout_width="200dp"
      android:layout_height="60dp"
      android:layout_marginTop="50dp"
      android:text="FitnessTracker"
      android:textAppearance="@style/TextAppearance.AppCompat.Large"
      android:gravity="center"
      app:layout_constraintEnd_toEndOf="parent"
      app:layout_constraintHorizontal_bias="0.5"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintTop_toTopOf="parent" />
  <!--
      <androidx.recyclerview.widget.RecyclerView
          android:id="@+id/recycler_view"
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          tools:listitem="@layout/note_item"
          tools:ignore="MissingConstraints" />
          -->

  <Button
      android:id="@+id/openWorkoutsButton"
      android:layout_marginLeft="8dp"
      android:layout_width="190dp"
      android:layout_height="200dp"
      android:text="Workouts"
      app:layout_constraintBottom_toBottomOf="parent"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintTop_toTopOf="parent" />

  <Button
      android:id="@+id/openProgressButton"
      android:layout_marginRight="8dp"
      android:layout_width="190dp"
      android:layout_height="200dp"
      android:text="Weekly Progress"
      app:layout_constraintBottom_toBottomOf="parent"
      app:layout_constraintEnd_toEndOf="parent"
      app:layout_constraintTop_toTopOf="parent" />

  <Button
      android:id="@+id/openGoalsButton"
      android:layout_marginRight="8dp"
      android:layout_width="190dp"
      android:layout_height="200dp"
      android:text="Goals"
      app:layout_constraintBottom_toBottomOf="parent"
      app:layout_constraintEnd_toEndOf="parent"
      app:layout_constraintTop_toBottomOf="@+id/openProgressButton"/>

  <Button
      android:id="@+id/openMaxLiftButton"
      android:layout_marginLeft="8dp"
      android:layout_width="190dp"
      android:layout_height="200dp"
      android:text="Max Lift"
      app:layout_constraintBottom_toBottomOf="parent"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintTop_toBottomOf="@+id/openWorkoutsButton"/>

</androidx.constraintlayout.widget.ConstraintLayout>